name: Publish to crates.io

# This crate is pure Rust: no build script, no dependencies. The published package
# is src/ + the golden-vector tests + README/ROADMAP/LICENSE (the AI dev guide and
# CI config are excluded via [package.exclude] in Cargo.toml), so the normal
# `cargo publish` verification build pulls in nothing.
#
# Flow: tag the release (vX.Y.Z, matching Cargo.toml), then dispatch this workflow
# with that tag (dry_run first, then for real). Requires the CARGO_REGISTRY_TOKEN
# repository secret.

on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag to publish (must already exist, e.g. v0.131.0)'
        required: true
      dry_run:
        description: 'Validate only — do not publish'
        type: boolean
        default: false

permissions:
  contents: read

jobs:
  preflight:
    name: Preflight (version matches tag, slim tarball)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event.inputs.tag }}

      - uses: dtolnay/rust-toolchain@stable

      - name: Verify Cargo.toml version matches tag
        env:
          INPUT_TAG: ${{ github.event.inputs.tag }}
        run: |
          set -euo pipefail
          tag="$INPUT_TAG"
          tag_no_v="${tag#v}"
          crate_version=$(grep -E '^version = ' Cargo.toml | head -1 | sed 's/.*"\(.*\)".*/\1/')
          if [ "$tag_no_v" != "$crate_version" ]; then
            echo "::error::Tag $tag (=$tag_no_v) does not match Cargo.toml version $crate_version"
            exit 1
          fi
          echo "Tag and version agree: $tag_no_v"

      - name: Verify slim tarball contents
        run: |
          set -euo pipefail
          cargo package --locked --list > /tmp/package-files.txt
          echo "--- Files in tarball ---"
          cat /tmp/package-files.txt
          # Internal/dev files must never ship (enforced by [package.exclude]): the
          # AI dev guide and CI config. cref/ + build.rs are kept as defensive
          # denials even though the C oracle has been removed from the repo.
          if grep -E '^(CLAUDE\.md|\.github/|cref/|build\.rs|target/)' /tmp/package-files.txt; then
            echo "::error::Internal files leaked into the package — check [package.exclude] in Cargo.toml."
            exit 1
          fi
          # Cap the tarball so a stray large file (the vendored C, target/, …) fails
          # the release; the real package is tens of KiB.
          cargo package --no-verify --locked
          size=$(stat -c%s target/package/zlib-bitexact-rs-*.crate)
          echo "Tarball size: $size bytes"
          if [ "$size" -gt 524288 ]; then
            echo "::error::Tarball exceeds 512 KiB cap."
            exit 1
          fi

      - name: Upload tarball as artifact
        uses: actions/upload-artifact@v6
        with:
          name: crate-tarball
          path: target/package/zlib-bitexact-rs-*.crate

  publish:
    name: cargo publish
    needs: preflight
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event.inputs.tag }}

      - uses: dtolnay/rust-toolchain@stable

      - name: Publish
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
          DRY_RUN: ${{ github.event.inputs.dry_run }}
        run: |
          set -euo pipefail
          if [ "$DRY_RUN" = "true" ]; then
            cargo publish --dry-run --locked
          else
            cargo publish --locked
          fi
